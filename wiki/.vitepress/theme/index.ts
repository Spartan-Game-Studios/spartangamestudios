import DefaultTheme from 'vitepress/theme';
import './custom.css';

// The stock VitePress layout, re-skinned to the studio's palette and type in
// custom.css. Keeping the default layout means new VitePress features (search,
// outline, edit links) come for free.
export default DefaultTheme;
